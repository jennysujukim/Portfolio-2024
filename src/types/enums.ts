export enum BreakpointType {
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
}

export enum CategoryType {
  ALL = 'All',
  DESIGN = 'Design',
  DEVELOP = 'Develop'
}

export enum ContentType {
  VIDEO = 'video',
  IMAGE = 'image'
}